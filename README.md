# 颜丰个人学术主页

## 目录结构

```text
.
├── index.html
├── assets/
│   └── profile.jpg
└── blog/
    └── index.html
```

## 发布到 GitHub Pages

1. 将本目录中的全部文件上传到 `yanfeng918.github.io` 仓库根目录。
2. 在仓库中进入 `Settings → Pages`。
3. 选择 `Deploy from a branch`，分支选择 `main`，目录选择 `/ (root)`。
4. 保存后等待 GitHub 自动发布。

以后修改 `index.html` 并提交到 `main` 分支，网页会自动更新。yanfeng